# Быстрая инструкция по публикации релиза

## 🚀 Быстрый старт (5 минут)

### 1. Обновите версию
```powershell
# Отредактируйте version.py
__version__ = "1.0.1"
```

### 2. Соберите приложение
```powershell
.\build.bat
.\build-installer.bat
```

### 3. Создайте тег и релиз на GitHub

**Вариант A: Через веб-интерфейс (рекомендуется)**
1. Перейдите: https://github.com/rbait31/ChatList/releases/new
2. Выберите "Choose a tag" → "Create new tag" → `v1.0.1`
3. Заголовок: `ChatList v1.0.1`
4. Скопируйте содержимое из `.github/release_template.md` и замените `{VERSION}` на `1.0.1`
5. Загрузите файл: `installer\ChatList-Setup-1.0.1.exe`
6. Нажмите "Publish release"

**Вариант B: Через командную строку**
```powershell
git tag -a v1.0.1 -m "Release version 1.0.1"
git push origin v1.0.1
# Затем создайте релиз через веб-интерфейс
```

### 4. Обновите лендинг (опционально)
```powershell
# Отредактируйте docs/index.html (версия в шапке)
git add docs/index.html
git commit -m "Обновление лендинга для версии 1.0.1"
git push
```

## 📋 Чек-лист

- [ ] Версия обновлена в `version.py`
- [ ] Приложение собрано (`dist\ChatList-{версия}.exe`)
- [ ] Инсталлятор создан (`installer\ChatList-Setup-{версия}.exe`)
- [ ] Тест установки пройден
- [ ] Тег создан (`v{версия}`)
- [ ] Release создан на GitHub
- [ ] Файлы загружены в Release
- [ ] Лендинг обновлен (если нужно)

## 📚 Подробная инструкция

См. файл [RELEASE.md](RELEASE.md) для подробной инструкции.

